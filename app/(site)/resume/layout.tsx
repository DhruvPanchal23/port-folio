export const metadata = {
  title: 'Resume',
  description: 'Experience, skills, and education of Dhruv Panchal',
};

export default function ResumeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
