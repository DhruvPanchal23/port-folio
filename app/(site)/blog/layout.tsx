export const metadata = {
  title: 'Blog',
  description: 'Thoughts, ideas, and stories from Dhruv Panchal',
};

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return children;
}
